const path = require("path");

module.exports = {
  src: path.resolve(__dirname, "../src"), // Source files
  build: path.resolve(__dirname, "../build"), // Build files
  public: path.resolve(__dirname, "../public"), // Static files
};
